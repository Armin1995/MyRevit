<Window x:Class="RevitExportTool.View.RevitExportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitExportTool.View"
        mc:Ignorable="d"
        Title="Revit导出工具" Height="450" Width="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="请选择需要导出的Revit模型(.rvt格式):"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding RevitFilePath}" IsReadOnly="True" />
        <Button Grid.Row="0" Grid.Column="2" Content="..." Command="{Binding SelectRevitFileCommand}"/>
    </Grid>
</Window>
